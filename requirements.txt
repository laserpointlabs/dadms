# Python 3.10.6
camunda-external-task-client-python3==4.5.0
openai==1.83.0
neo4j==5.16.0
requests==2.31.0
colorama==0.4.6
flask==3.0.1
tabulate==0.9.0
qdrant-client==1.7.3

# CPU-only ML dependencies for faster builds
# Install PyTorch CPU-only first to avoid GPU package downloads
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu
torchaudio==2.1.0+cpu

# Sentence transformers with CPU-only torch backend
sentence-transformers==2.2.2

# Hugging Face Hub
huggingface_hub==0.25.2

# Service discovery
python-consul==1.1.0
